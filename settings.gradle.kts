plugins {
	// Downloads the Java 25 toolchain the build asks for when no matching JDK is installed.
	id("org.gradle.toolchains.foojay-resolver-convention") version "1.0.0"
}

rootProject.name = "mcphub"
